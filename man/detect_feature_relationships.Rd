% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_feature_network.R
\name{detect_feature_relationships}
\alias{detect_feature_relationships}
\title{Detect isotope, adduct, and in-source fragment relationships}
\usage{
detect_feature_relationships(
  object,
  mode = c("positive", "negative"),
  detect = c("isotope", "adduct", "isf"),
  ppm = 10,
  rt_tolerance = 1,
  cor_cutoff = 0.7,
  cor_method = "pearson",
  max_charge = 2,
  max_neutral_loss_charge = 1,
  isotope_intensity_ratio_max = 0.8,
  adduct_table = NULL,
  neutral_loss_table = NULL,
  use_ms2 = TRUE,
  ms2_mz_tol_ppm = 5,
  ms2_rt_tol = 10,
  ms2_fragment_mz_tol = 0.02,
  store = TRUE
)
}
\arguments{
\item{object}{A `mass_dataset` object.}

\item{mode}{Ion mode. One of `"positive"` or `"negative"`.}

\item{detect}{Relationship classes to detect.}

\item{ppm}{Mass tolerance in ppm.}

\item{rt_tolerance}{Retention time tolerance in seconds.}

\item{cor_cutoff}{Abundance correlation used as the full-support reference
for edge scoring. It is not used as a hard filter for within-mode edges.}

\item{cor_method}{Correlation method passed to `stats::cor()`.}

\item{max_charge}{Maximum charge state checked for isotopes and neutral loss.}

\item{max_neutral_loss_charge}{Maximum charge state checked for neutral
losses. For plant small-molecule LC-MS data, `1` is a conservative default
that avoids ambiguous edges such as dihexose/z2 versus hexose/z1.}

\item{isotope_intensity_ratio_max}{Maximum mean abundance ratio of isotope to
monoisotopic peak.}

\item{adduct_table}{Optional adduct dictionary with columns `annotation`,
`mass_offset`, and optional `charge`.}

\item{neutral_loss_table}{Optional neutral loss dictionary with columns
`annotation` and `mass`.}

\item{use_ms2}{Logical. If `TRUE`, add MS2 assignment-aware evidence for
ISF edges when spectra are available in `object@ms2_data`.}

\item{ms2_mz_tol_ppm}{Strict MS1-to-MS2 precursor m/z tolerance used to
re-audit `mutate_ms2()` assignments before using MS2 evidence.}

\item{ms2_rt_tol}{Strict MS1-to-MS2 precursor RT tolerance in seconds used
to re-audit `mutate_ms2()` assignments before using MS2 evidence.}

\item{ms2_fragment_mz_tol}{Absolute m/z tolerance used when checking whether
an ISF feature m/z appears in a parent MS2 spectrum.}

\item{store}{Logical. If `TRUE`, attach the network to `object@other_files`.}
}
\value{
If `store = TRUE`, an updated `mass_dataset`; otherwise an edge table.
}
\description{
Detect isotope, adduct, and in-source fragment relationships
}
